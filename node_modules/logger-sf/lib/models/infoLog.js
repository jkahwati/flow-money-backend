const InfoDetail = require('./infoDetail');
const Log = require('./Log');

class InfoLog extends Log{
    constructor() {
        super();
        this.details = undefined;
    }
    // withDetails (duration, status, url) {
    //     let infoDetail = new InfoDetail(duration, status, url);
    //     this.details = infoDetail;
    //     return this;
    // }
}

module.exports = InfoLog;
